{
  "name": "village-mandi-root",
  "private": true,
  "workspaces": [
    "web",
    "server",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspace=web && rm -rf public && mv web/dist public && npm run prisma:generate --workspace=server",
    "dev": "npm run dev --workspace=web"
  },
  "dependencies": {
    "fastify": "^5.6.2",
    "pino": "^10.1.1",
    "pino-pretty": "^13.1.3",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@prisma/client": "^6.19.2",
    "@types/node": "^25.0.7",
    "prisma": "^6.19.2",
    "typescript": "^5.9.3"
  }
}
