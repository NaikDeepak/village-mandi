---
phase: 25-security-and-reliability-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/src/routes/auth.ts, web/src/components/auth/AuthProvider.tsx, server/src/middleware/auth.ts, docs/todo.md, web/src/App.tsx, web/src/pages/AdminDashboardPage.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "AuthProvider recovers gracefully from API failures"
    - "Security logs do not contain raw headers"
    - "Admin login timing is consistent"
    - "Duplicate code and outdated todos are removed"
  artifacts:
    - path: "web/src/components/auth/AuthProvider.tsx"
      provides: "Robust auth loading state management"
    - path: "server/src/routes/auth.ts"
      provides: "Secure admin login and logging"
  key_links: []
---

<objective>
Refine security and reliability by fixing prioritized issues (P0/P1) and cleaning up tech debt.

Purpose: Harden the application for production by preventing enumeration attacks, ensuring UI stability, and removing sensitive data from logs.
Output: Patched auth components, sanitized logs, and updated documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/src/routes/auth.ts
@web/src/components/auth/AuthProvider.tsx
@server/src/middleware/auth.ts
@docs/todo.md
@web/src/App.tsx
@web/src/pages/AdminDashboardPage.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fix AuthProvider Stability (BUG-001)</name>
  <files>web/src/components/auth/AuthProvider.tsx</files>
  <action>
    Wrap `checkAuth` logic in `try/finally` block.
    Ensure `setLoading(false)` is ALWAYS called, even if `authApi.me()` throws.
    Prevent "infinite loading" state on network errors.
  </action>
  <verify>grep "finally" web/src/components/auth/AuthProvider.tsx</verify>
  <done>AuthProvider handles errors gracefully</done>
</task>

<task type="auto">
  <name>Task 2: Sanitize Security Logs (SEC-006)</name>
  <files>server/src/middleware/auth.ts, server/src/routes/auth.ts</files>
  <action>
    Remove or sanitize `request.headers` logging in `authenticate` middleware and `firebase-verify` route.
    Ensure no Authorization tokens or cookies are written to logs.
    Use `request.id` or similar safe context instead.
  </action>
  <verify>grep -v "request.headers" server/src/middleware/auth.ts</verify>
  <done>No raw headers in logs</done>
</task>

<task type="auto">
  <name>Task 3: Prevent Account Enumeration (SEC-007)</name>
  <files>server/src/routes/auth.ts</files>
  <action>
    Refactor `/auth/admin/login` to use constant-time comparison flow.
    Always run `verifyPassword` even if user is not found (against a dummy hash).
    Ensure "Invalid credentials" error is identical for both cases (timing and content).
  </action>
  <verify>Check logic flow ensures verifyPassword is called in all paths</verify>
  <done>Timing attack vector closed</done>
</task>

<task type="auto">
  <name>Task 4: Clean up Tech Debt & Documentation</name>
  <files>docs/todo.md, web/src/App.tsx, web/src/pages/AdminDashboardPage.tsx</files>
  <action>
    1. Remove duplicate `{/* Protected admin routes */}` comment in `web/src/App.tsx` (CODE-001).
    2. Remove duplicate header in `web/src/pages/AdminDashboardPage.tsx` (UX-002) - keep only dashboard content.
    3. Update `docs/todo.md`: Mark resolved items (SEC-001-004, BUG-002, CODE-001, UX-002) as Complete.
  </action>
  <verify>grep -v "Protected admin routes" web/src/App.tsx | wc -l</verify>
  <done>Tech debt resolved and tracked</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] AuthProvider has try/finally
- [ ] No raw header logging
- [ ] Admin login returns generic error with constant timing (logic check)
- [ ] Todo list updated
</verification>

<success_criteria>
- Critical P0/P1 issues resolved
- Codebase cleaner
</success_criteria>

<output>
After completion, create .planning/phases/25-security-and-reliability-refinement/25-01-SUMMARY.md
</output>
