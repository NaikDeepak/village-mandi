---
phase: 27-identity-security-foundation
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified: [web/src/context/AuthProvider.tsx, web/src/hooks/usePhoneAuth.ts, web/src/pages/auth/WaitlistPage.tsx, web/src/pages/auth/RejectedPage.tsx]
autonomous: false
must_haves:
  truths:
    - "Blocked users see a Waitlist page if pending"
    - "Blocked users see a Rejected page if rejected"
  artifacts:
    - path: "web/src/pages/auth/WaitlistPage.tsx"
      provides: "Friendly waitlist UI"
---

<objective>
Update the React frontend to catch specific Firebase Auth blocking errors and redirect users to appropriate "Waitlist" or "Rejected" informational pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@web/src/context/AuthProvider.tsx
@web/src/hooks/usePhoneAuth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handle Auth Errors in Hooks</name>
  <files>web/src/hooks/usePhoneAuth.ts, web/src/context/AuthProvider.tsx</files>
  <action>
    Update 'confirmOtp' logic to catch errors from the Firebase SDK.
    Map 'ACCOUNT_PENDING' error message to a specific UI state or redirect.
    Parse 'ACCOUNT_REJECTED' error which may contain a JSON string with a 'reason' field.
    Ensure AuthProvider doesn't treat these errors as generic system failures.
  </action>
  <verify>Simulate a blocking error in the login flow</verify>
  <done>Frontend can distinguish between blocking reasons and handle rejection reasons</done>
</task>

<task type="auto">
  <name>Task 2: Create Informational Pages</name>
  <files>web/src/pages/auth/WaitlistPage.tsx, web/src/pages/auth/RejectedPage.tsx</files>
  <action>
    Create 'WaitlistPage' with a "Thank you for registering" message and "Check back later" instructions.
    Create 'RejectedPage' with a brief message about the application status, displaying the 'rejectionReason' if passed from the auth hook.
    Add these routes to the main App router (publicly accessible or via specific auth state).
  </action>
  <verify>Manually navigate to /waitlist and /rejected</verify>
  <done>UI components for blocked states exist and display reasons</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end blocking experience</what-built>
  <how-to-verify>
    1. Manually set a user to 'PENDING' in the database.
    2. Attempt to login via phone in the web UI.
    3. Verify user is redirected to the /waitlist page.
    4. Set user to 'REJECTED' with a specific 'rejectionReason' and verify redirection to /rejected showing that reason.
    5. Set user to 'APPROVED' and verify successful login to Dashboard.
  </how-to-verify>
  <resume-signal>Type "verified" after testing the flows</resume-signal>
</task>

</tasks>

<success_criteria>
Users receive clear, branded feedback when their access is restricted by the waitlist/approval system.
</success_criteria>
