---
phase: 27-identity-security-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/prisma/schema.prisma, shared/constants.ts, server/scripts/migrate-legacy-users.ts]
autonomous: true
must_haves:
  truths:
    - "User model has a RegistrationStatus field"
    - "Existing users in the database are marked as APPROVED"
  artifacts:
    - path: "server/prisma/schema.prisma"
      provides: "RegistrationStatus enum"
    - path: "server/scripts/migrate-legacy-users.ts"
      provides: "One-time migration logic"
---

<objective>
Update the database schema to support registration states (PENDING, APPROVED, REJECTED) and ensure existing production users are grandfathered into the APPROVED state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-identity-security-foundation/27-CONTEXT.md
@server/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Prisma Schema</name>
  <files>server/prisma/schema.prisma</files>
  <action>
    Add 'enum RegistrationStatus { PENDING, APPROVED, REJECTED }' to the schema.
    Add 'status RegistrationStatus @default(PENDING)' to the User model.
    Add 'rejectionReason String?' to the User model to support explaining why an account was not approved.
    Generate Prisma client.
  </action>
  <verify>npx prisma validate && npx prisma generate</verify>
  <done>User model includes status and rejectionReason fields</done>
</task>

<task type="auto">
  <name>Task 2: Update Shared Constants</name>
  <files>shared/constants.ts</files>
  <action>
    Add RegistrationStatus to shared constants or types if they exist, ensuring frontend and backend stay in sync.
  </action>
  <verify>Check file content for RegistrationStatus export</verify>
  <done>RegistrationStatus is available globally</done>
</task>

<task type="auto">
  <name>Task 3: Legacy Migration Script</name>
  <files>server/scripts/migrate-legacy-users.ts</files>
  <action>
    Create a script to set 'status: APPROVED' for all users where 'isInvited: true' or role is 'ADMIN'.
    This prevents locking out existing users during the transition.
    Execute the script using ts-node or similar.
  </action>
  <verify>Run script and verify count of updated users</verify>
  <done>All current users are grandfathered into APPROVED status</done>
</task>

</tasks>

<success_criteria>
Prisma schema is updated and database is migrated without breaking existing user access.
</success_criteria>
