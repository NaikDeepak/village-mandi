---
phase: 22-auth-flow-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [web/src/lib/api.ts, web/src/components/auth/AuthProvider.tsx, web/src/components/auth/PhoneLoginForm.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "API returns typed UserRole"
    - "Components don't need role casting"
    - "AuthProvider handles hydration efficiently"
  artifacts:
    - path: "web/src/lib/api.ts"
      provides: "Strongly typed API client"
  key_links:
    - from: "api.ts"
      to: "AuthProvider.tsx"
      via: "UserRole type"
---

<objective>
Refactor API client and auth components for strict type safety.

Purpose: Remove unsafe "as" casting and improve hydration UX.
Output: Strongly typed API client and updated components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@web/src/lib/api.ts
@web/src/components/auth/AuthProvider.tsx
@web/src/components/auth/PhoneLoginForm.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Enforce UserRole in API</name>
  <files>web/src/lib/api.ts</files>
  <action>
    Update `authApi` methods to use `UserRole` type explicitly instead of string.
    Ensure return types for login/me/verify endpoints carry the correct role union type.
  </action>
  <verify>grep "UserRole" web/src/lib/api.ts</verify>
  <done>API returns strict role types</done>
</task>

<task type="auto">
  <name>Task 2: Cleanup Component Casting</name>
  <files>web/src/components/auth/AuthProvider.tsx, web/src/components/auth/PhoneLoginForm.tsx</files>
  <action>
    Remove `as 'ADMIN' | 'BUYER'` casting.
    Optimistically check persisted store state in `AuthProvider` before `me()` call completes to prevent UI flicker.
  </action>
  <verify>grep -v "as 'ADMIN' | 'BUYER'" web/src/components/auth/AuthProvider.tsx</verify>
  <done>Components rely on inference, not casting</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] No explicit role casting in auth components
- [ ] API methods return UserRole
- [ ] AuthProvider hydration is smooth
</verification>

<success_criteria>
- Type safety improved
- UX flicker reduced
</success_criteria>

<output>
After completion, create .planning/phases/22-auth-flow-cleanup/22-02-SUMMARY.md
</output>
