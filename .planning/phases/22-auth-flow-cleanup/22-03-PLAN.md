---
phase: 22-auth-flow-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [web/src/pages/AdminLoginPage.tsx, web/src/lib/api.ts, web/src/components/auth/AuthProvider.tsx]
autonomous: true
gap_closure: true
user_setup: []
must_haves:
  truths:
    - "Admin login page uses inferred types without casting"
    - "API client enforces required phone string"
    - "AuthProvider passes phone directly to store"
  artifacts:
    - path: "web/src/pages/AdminLoginPage.tsx"
      provides: "Type-safe admin login"
  key_links: []
---

<objective>
Close remaining type safety gaps in auth flow.

Purpose: Eliminate manual casting and fix type mismatches for 'phone' field.
Output: Fully typed auth components and API client.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-auth-flow-cleanup/22-auth-flow-cleanup-VERIFICATION.md
@web/src/pages/AdminLoginPage.tsx
@web/src/lib/api.ts
@web/src/components/auth/AuthProvider.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Remove Casting in Admin Login</name>
  <files>web/src/pages/AdminLoginPage.tsx</files>
  <action>
    Remove `as 'ADMIN' | 'BUYER'` casting on line 56.
    Ensure `result.data.user.role` is correctly inferred from the API client return type.
  </action>
  <verify>grep -v "as 'ADMIN' | 'BUYER'" web/src/pages/AdminLoginPage.tsx</verify>
  <done>Explicit casting removed</done>
</task>

<task type="auto">
  <name>Task 2: Align Phone Nullability</name>
  <files>web/src/lib/api.ts, web/src/components/auth/AuthProvider.tsx</files>
  <action>
    1. Update `authApi.me` in `web/src/lib/api.ts` to mark `phone` as `string | null` explicitly (matching backend/store), or `string` if guaranteed. Ideally match `User` interface in store.
    2. Update `AuthProvider.tsx` to pass `phone` directly without `?? null` fallback if types align.
  </action>
  <verify>grep "phone" web/src/components/auth/AuthProvider.tsx</verify>
  <done>Phone types match between API and Store</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] No `as` casting in auth pages
- [ ] No type errors in AuthProvider
</verification>

<success_criteria>
- All gaps from verification report closed
- Clean build
</success_criteria>

<output>
After completion, create .planning/phases/22-auth-flow-cleanup/22-03-SUMMARY.md
</output>
