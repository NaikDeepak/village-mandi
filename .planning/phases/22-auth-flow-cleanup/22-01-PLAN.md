---
phase: 22-auth-flow-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/src/stores/auth.ts, server/src/schemas/auth.ts, web/src/lib/utils.ts, server/src/utils/phone.ts, server/src/routes/auth.ts, web/src/components/auth/PhoneLoginForm.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Phone normalization logic is centralized"
    - "User types match between frontend and backend"
    - "Logout relies on Zustand persistence"
  artifacts:
    - path: "web/src/lib/utils.ts"
      provides: "Client-side phone normalization"
    - path: "server/src/utils/phone.ts"
      provides: "Server-side phone normalization"
  key_links:
    - from: "auth.ts (store)"
      to: "auth.ts (schema)"
      via: "Consistent User type"
---

<objective>
Standardize auth types and centralize utilities to reduce technical debt.

Purpose: Eliminate friction from type mismatches and redundant normalization logic.
Output: Cleaned up stores, schemas, and utility files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@web/src/stores/auth.ts
@server/src/schemas/auth.ts
@web/src/components/auth/PhoneLoginForm.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Standardize User Type</name>
  <files>web/src/stores/auth.ts, server/src/schemas/auth.ts</files>
  <action>
    Align `web/src/stores/auth.ts` User interface with backend schema.
    Make `phone` required string (remove null) and `email` nullable string in store to match Zod schema.
    Ensure `role` uses the `UserRole` type alias.
    Remove any redundant logic in `logout` if present (Zustand persist handles it).
  </action>
  <verify>grep "phone: string" web/src/stores/auth.ts</verify>
  <done>Store types match backend schema exactly</done>
</task>

<task type="auto">
  <name>Task 2: Centralize Phone Normalization</name>
  <files>web/src/lib/utils.ts, server/src/utils/phone.ts, server/src/routes/auth.ts, web/src/components/auth/PhoneLoginForm.tsx</files>
  <action>
    1. Create `server/src/utils/phone.ts` with `normalizePhone` function (removes non-digits, extracts last 10).
    2. Add similar `normalizePhone` to `web/src/lib/utils.ts` (or create if missing).
    3. Update `server/src/routes/auth.ts` to use `normalizePhone` instead of inline regex.
    4. Update `web/src/components/auth/PhoneLoginForm.tsx` to use `normalizePhone` for input handling.
  </action>
  <verify>grep "normalizePhone" server/src/routes/auth.ts</verify>
  <done>Phone logic is DRY and consistent</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] User type in store has required phone field
- [ ] Backend uses utility for phone normalization
- [ ] Frontend uses utility for phone normalization
</verification>

<success_criteria>
- No duplicate phone logic
- Types are strictly aligned
- Logout is cleaner
</success_criteria>

<output>
After completion, create .planning/phases/22-auth-flow-cleanup/22-01-SUMMARY.md
</output>
