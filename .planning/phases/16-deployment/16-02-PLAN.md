---
phase: 16-deployment
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified: []
autonomous: false
---

<objective>
Deploy to production environment on Vercel and verify.

Purpose: Make the application live for users.
Output: Deployed and verified production application.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
@./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-deployment/16-01-SUMMARY.md
</context>

<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <action>Set up production database and Vercel project</action>
  <instructions>
    1. Create a new Supabase project (or use existing).
    2. Get the Transaction Connection Pooler string (Port 6543) for DATABASE_URL.
    3. Get the Direct Connection string (Port 5432) for DIRECT_URL.
    4. Create a new Vercel project importing this repo.
    5. In Vercel Project Settings > Environment Variables, add all keys from `.env.production.example`.
    6. Ensure `DATABASE_URL` and `DIRECT_URL` are set correctly.
    7. Deploy the project.
  </instructions>
  <verification>Vercel deployment URL is live</verification>
  <resume-signal>Type "done" when deployment is successful and provide the URL</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Production Deployment</what-built>
  <how-to-verify>
    1. Visit the production URL.
    2. Log in with the Admin credentials seeded.
    3. Verify the Dashboard loads.
    4. Verify functionality (create a batch, add a farmer).
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe issues</resume-signal>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Deployment is live
- [ ] Admin login works
- [ ] Database connection works
</verification>

<success_criteria>
- Application is running in production
- Database connected and seeded
- Core features verified in production
</success_criteria>

<output>
After completion, create `.planning/phases/16-deployment/16-02-SUMMARY.md`
</output>
