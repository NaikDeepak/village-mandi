---
phase: 26-add-testcases-for-uncovered-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/src/routes/auth.test.ts, server/src/routes/users.test.ts, server/src/routes/logs.test.ts, web/src/hooks/usePhoneAuth.test.ts]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Auth routes pass validation and logic checks"
    - "User invite flow works as expected"
    - "Log routes handle communication logs correctly"
    - "usePhoneAuth hook manages state and errors correctly"
  artifacts:
    - path: "server/src/routes/auth.test.ts"
      provides: "Tests for admin login, logout, and firebase verify"
    - path: "server/src/routes/users.test.ts"
      provides: "Tests for user invite flow"
    - path: "server/src/routes/logs.test.ts"
      provides: "Tests for communication logs"
    - path: "web/src/hooks/usePhoneAuth.test.ts"
      provides: "Tests for phone auth hook"
  key_links: []
---

<objective>
Add comprehensive test cases for currently uncovered critical paths in server and web.

Purpose: Improve test coverage and system reliability by testing auth flows, user management, and logging.
Output: New test files for auth, users, logs routes, and phone auth hook.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/src/routes/auth.ts
@server/src/routes/users.ts
@server/src/routes/logs.ts
@web/src/hooks/usePhoneAuth.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add tests for Auth routes</name>
  <files>server/src/routes/auth.test.ts</files>
  <action>Create test file for auth routes covering:
  - POST /auth/admin/login (success, failure scenarios)
  - GET /auth/me (success, not found)
  - POST /auth/logout
  - POST /auth/firebase-verify (validation, verification logic, user creation/linking)
  Mock firebase admin and prisma calls.
  </action>
  <verify>npm run test --workspace=server src/routes/auth.test.ts</verify>
  <done>All auth route tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for User routes</name>
  <files>server/src/routes/users.test.ts</files>
  <action>Create test file for user routes covering:
  - POST /users/invite (admin only, validation, upsert logic)
  Mock prisma calls.
  </action>
  <verify>npm run test --workspace=server src/routes/users.test.ts</verify>
  <done>User invite tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for Log routes</name>
  <files>server/src/routes/logs.test.ts</files>
  <action>Create test file for log routes covering:
  - POST /logs/communication (validation, creation)
  - GET /logs/communication/:entityType/:entityId (retrieval)
  Mock prisma calls.
  </action>
  <verify>npm run test --workspace=server src/routes/logs.test.ts</verify>
  <done>Log route tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Add tests for usePhoneAuth hook</name>
  <files>web/src/hooks/usePhoneAuth.test.ts</files>
  <action>Create test file for usePhoneAuth hook covering:
  - requestOtp (success, cooldown, error handling)
  - verifyOtp (success, error handling)
  - reset state
  Mock firebase auth functions.
  </action>
  <verify>npm run test --workspace=web src/hooks/usePhoneAuth.test.ts</verify>
  <done>Hook tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test:coverage --workspace=server shows improved coverage for auth, users, logs
- [ ] npm run test --workspace=web passes
</verification>

<success_criteria>
- All new test files created and passing
- Critical paths covered by tests
</success_criteria>

<output>
After completion, create .planning/phases/26-add-testcases-for-uncovered-code/26-01-SUMMARY.md
</output>
